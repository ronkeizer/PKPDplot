library(PKPDsim)
library(PKPDplot)

# p <- list(CL = 1, V  = 10, KA = 0.5)
# pk1 <- new_ode_model("pk_1cmt_oral")
# r1 <- new_regimen(amt = 100,
#                   times = c(0, 12, 24, 36))
# dat <- sim_ode (ode = "pk1",
#                 par = p,
#                 n = 50,
#                 omega = c(0.1, 0.05, 0.1),
#                 regimen = r1)
# plot(dat)
# plot(dat,
#      show = list(ci = TRUE, median = TRUE),
#      target = c(5, 11))
